# ============================================================
# AI Digital Tutor — Dev / Test Dependencies
# Install: pip install -r requirements.txt -r requirements-dev.txt
# ============================================================

-r requirements.txt

pytest>=8.0,<9
pytest-asyncio>=0.23,<1

# W2: coverage reporting + threshold gate
pytest-cov>=5,<7

# W2: real async Mongo semantics for integration tests without a live server
# (CI uses a real mongo:7 service via INTEGRATION_REAL_MONGO=1).
mongomock-motor>=0.0.3,<1
